# Project
<!-- safe-github-publisher:setup -->
## Local Setup

This repository does not include real secrets or local-only credentials.

### Install
- Run `npm install` to install dependencies.

### Configure
- Create any required local config files from the examples in this repository.

Never commit real API keys, passwords, private keys, database files, or local session files.
